<%- include('../partials/admin-header') %>

<!-- Dashboard Header -->
<div class="mb-8">
  <h1 class="text-3xl font-bold text-white">Dashboard</h1>
  <p class="mt-2 text-gray-400">Manage your video library and client presentations</p>
</div>

<!-- Stats Cards -->
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
  <div class="bg-gray-800 rounded-lg shadow p-6">
    <div class="flex items-center">
      <div class="p-3 bg-spindle-900 rounded-full">
        <svg class="w-6 h-6 text-spindle-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 10l4.553-2.276A1 1 0 0121 8.618v6.764a1 1 0 01-1.447.894L15 14M5 18h8a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v8a2 2 0 002 2z"></path>
        </svg>
      </div>
      <div class="ml-4">
        <p class="text-sm font-medium text-gray-400">Total Videos</p>
        <p class="text-2xl font-semibold text-white"><%= videoCount %></p>
      </div>
    </div>
  </div>

  <div class="bg-gray-800 rounded-lg shadow p-6">
    <div class="flex items-center">
      <div class="p-3 bg-blue-900 rounded-full">
        <svg class="w-6 h-6 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
        </svg>
      </div>
      <div class="ml-4">
        <p class="text-sm font-medium text-gray-400">Presentations</p>
        <p class="text-2xl font-semibold text-white"><%= presentationCount %></p>
      </div>
    </div>
  </div>
</div>

<!-- Quick Actions -->
<div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
  <a href="/admin/videos/new" class="bg-gray-800 rounded-lg shadow p-6 hover:bg-gray-700 transition-colors flex items-center">
    <div class="p-3 bg-green-900 rounded-full">
      <svg class="w-6 h-6 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
      </svg>
    </div>
    <div class="ml-4">
      <p class="text-lg font-medium text-white">Add New Video</p>
      <p class="text-sm text-gray-400">Add a video to your library</p>
    </div>
  </a>

  <a href="/admin/presentations/new" class="bg-gray-800 rounded-lg shadow p-6 hover:bg-gray-700 transition-colors flex items-center">
    <div class="p-3 bg-purple-900 rounded-full">
      <svg class="w-6 h-6 text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
      </svg>
    </div>
    <div class="ml-4">
      <p class="text-lg font-medium text-white">Create Presentation</p>
      <p class="text-sm text-gray-400">Build a new client presentation</p>
    </div>
  </a>
</div>

<!-- Recent Items -->
<div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
  <!-- Recent Videos -->
  <div class="bg-gray-800 rounded-lg shadow">
    <div class="px-6 py-4 border-b border-gray-700 flex justify-between items-center">
      <h2 class="text-lg font-medium text-white">Recent Videos</h2>
      <a href="/admin/videos" class="text-sm text-spindle-400 hover:text-spindle-300">View all</a>
    </div>
    <ul class="divide-y divide-gray-700">
      <% recentVideos.forEach(function(video) { %>
        <li class="px-6 py-4 hover:bg-gray-700">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-medium text-white"><%= video.title || video.project %></p>
              <p class="text-sm text-gray-400"><%= video.client %></p>
            </div>
            <a href="/admin/videos/<%= video._id %>/edit" class="text-sm text-gray-400 hover:text-gray-300">Edit</a>
          </div>
        </li>
      <% }); %>
      <% if (recentVideos.length === 0) { %>
        <li class="px-6 py-4 text-gray-400 text-sm">No videos yet</li>
      <% } %>
    </ul>
  </div>

  <!-- Recent Presentations -->
  <div class="bg-gray-800 rounded-lg shadow">
    <div class="px-6 py-4 border-b border-gray-700 flex justify-between items-center">
      <h2 class="text-lg font-medium text-white">Recent Presentations</h2>
      <a href="/admin/presentations" class="text-sm text-spindle-400 hover:text-spindle-300">View all</a>
    </div>
    <ul class="divide-y divide-gray-700">
      <% recentPresentations.forEach(function(pres) { %>
        <li class="px-6 py-4 hover:bg-gray-700">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-medium text-white"><%= pres.client %></p>
              <p class="text-sm text-gray-400"><%= pres.videos.length %> videos - <%= pres.layout %> layout</p>
            </div>
            <div class="flex items-center space-x-3">
              <a href="/p/<%= pres.slug %>" target="_blank" class="text-sm text-blue-400 hover:text-blue-300">View</a>
              <a href="/admin/presentations/<%= pres._id %>/edit" class="text-sm text-gray-400 hover:text-gray-300">Edit</a>
            </div>
          </div>
        </li>
      <% }); %>
      <% if (recentPresentations.length === 0) { %>
        <li class="px-6 py-4 text-gray-400 text-sm">No presentations yet</li>
      <% } %>
    </ul>
  </div>
</div>

<%- include('../partials/admin-footer') %>
